node_modules
.env
.env.*
!.env.example
*.log
dist
# Uploaded profile photos from a local dev run. Never baked into an image —
# these are real people's pictures on a developer's disk (see src/photos/).
.uploads
